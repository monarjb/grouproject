<%- include ../partials/header.ejs %>

<%- include ../partials/main_nav.ejs %>

<!-- Content for the Edit page -->
<div class="container">
    <div class="row">
        <div class="offset-md-3 col-md-6">
          <!--<h1><%= title %></h1>-->
            <form class="form" method="POST">
              <div class="form-group">
                <!--label-- for="NameTextField">Title</!--label-->
                <h2><%= title %></h2>
                
                <% for (i=1; i < 6; i++) { %> 

                  <div class="d-flex flex-column bd-highlight mb-3"></div>
                  <div class="h5 p-1 bd-highlight"><%= eval('survey.q' + i) %></div>

                  <% if ( survey.type == "True/False") { %>
                    <div class="form-check form-check-inline">
                      <input class="form-check-input" type="radio" name="a<%= i %>" id="radio1-<%= i %>" value="true" checked=true>
                      <label class="form-check-label" for="radio2-<%= i %>">True</label>
                    </div>
                    <div class="form-check form-check-inline">
                      <input class="form-check-input" type="radio" name="a<%= i %>" id="radio2-<%= i %>" value="false">
                      <label class="form-check-label" for="radio1-<%= i %>">False</label>
                    </div>

                  <% } %>

                  <% if ( survey.type == "Scale") { %>
                    <% let scale = {1:"very bad", 2:"bad", 3:"good", 4:"very good", 5:"excellent"}
                    <% for (j=1; j < 6; j++) { %> 
                      <div class="form-check form-check-inline">
                        <input class="form-check-input" type="radio" name="g<%= j %>" id="radio-<%= j %>" value="<%= j %>">
                        <label class="form-check-label" for="radio-<%= j %>"><%= j %></label>
                      </div>
                    <% } %>
                  <% } %>

                <% } %>




                

                
                

              <div>
                <button class="btn btn-primary " type="submit">
                  <i class="fas fa-edit"></i> Submit</button>
              
                  <a href="/survey-list" class="btn btn-warning">
                      <i class="fas fa-undo"></i> Cancel</a>
              </div>
              
            </form>

           
        </div>

    </div>
</div>

<br/><br/><br/>

<%- include ../partials/bottom_nav.ejs %>  

<%- include ../partials/footer.ejs %>